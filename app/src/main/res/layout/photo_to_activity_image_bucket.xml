<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f1eff5"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/title_height"
        android:background="@color/title_color"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <FrameLayout
            android:id="@+id/bucket_ly_back"
            android:layout_width="50dip"
            android:layout_height="match_parent" >

            <ImageView
                style="@style/imgIconCommon"
                android:layout_gravity="center"
                android:src="@mipmap/back" />
        </FrameLayout>

        <TextView
            android:id="@+id/ugptitle_tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="我的相册"
            android:textColor="#ffffff"
            android:textSize="20sp" />

        <FrameLayout
            android:id="@+id/ugptitle_click_right"
            android:layout_width="@dimen/distance_50"
            android:layout_height="match_parent" >
        </FrameLayout>
    </LinearLayout>

    <GridView
        android:id="@+id/gridview_bucket"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp"
        android:horizontalSpacing="20dp"
        android:numColumns="2"
        android:scrollbars="none"
        android:verticalSpacing="20dp" >
    </GridView>

</LinearLayout>